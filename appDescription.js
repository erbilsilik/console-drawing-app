console.log('\nN w h Create a new canvas of width w and height h.\n');

console.log(`L x1 y1 x2 y2 Create a new line from (x1,y1) to (x2,y2). Currently only horizontal or vertical
   lines are supported. Horizontal and vertical lines will be drawn using the selected colour.\n`);

console.log(`R x1 y1 x2 y2 Create a new rectangle, whose upper left corner is (x1,y1) and lower right corner 
  is (x2,y2). Horizontal and vertical lines will be drawn using the selected colour.\n`);

console.log(`B x y Fill the entire area connected to (x,y) with selected colour. 
  The behaviour of this is the same as that of the "bucket fill" tool in paint programs.\n`);

console.log('Set the selected colour to “c”. The default colour is “x”.\n');

console.log('Q Quit the program.\n');
