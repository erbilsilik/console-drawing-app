{
  "name": "erbil_silik_evenito_coding_problem",
  "version": "1.0.0",
  "description": "Console drawing application",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "test": "jest",
    "docker-start": "docker-compose up -d && docker exec -it console-drawing-app sh -c \"npm start\"",
    "docker-test": "docker-compose up -d && docker exec -it console-drawing-app sh -c \"npm test\""
  },
  "author": "Erbil Silik",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
